import { MAP_BASE64_DATA } from './mapBase64Data';

// To render the MB1 map, replace the value below with the actual base64 representation of the floor plan image 
// (e.g. export const MB1_MAP_BASE64 = 'data:image/png;base64,iVBORw0KGgo...')
export const MB1_MAP_BASE64 = MAP_BASE64_DATA.MB1_GR; 
